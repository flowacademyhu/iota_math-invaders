const {generateMap, fillMap, printMap, playerMove, hit, gamerator, numbersMove, bulletsMove} = require('./map');

const main = () => {
    gamerator();
    fillMap();
    printMap();
    

}

main();

